import React, { useState } from "react";

const Modal = ({ slides }) => {
    return (
        <>
            <div>
                {
                    slides.forEach(element => {
                        
                    })
                }
            </div>
        </>

    );
};

export default Modal;